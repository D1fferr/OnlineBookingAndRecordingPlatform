spring:
  application:
    name: ProvidersService
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
  profiles:
    active: local
server:
  port: 8083
