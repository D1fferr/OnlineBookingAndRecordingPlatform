external:
  kafka:
    endpoint: "${KAFKA_ENDPOINT}"
  mail:
    login: "${MAIL_LOGIN}"
    password: "${MAIL_PASSWORD}"
    from: "${MAIL_FROM}"
    host: "${MAIL_HOST}"
    port: "${MAIL_PORT}"
  services:
    gateway: "${GETAWAY_URL}"